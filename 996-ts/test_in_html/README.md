
---

```markdown
# TypeScript + HTML Quick Start

This project demonstrates how to use **TypeScript** with a simple HTML page.

---

## 📂 Project Structure
```
project-folder/
│
├── index.html   # HTML file that loads the compiled JS
├── hello.ts     # TypeScript source file
└── hello.js     # JavaScript output (generated by tsc)
```

---

## 📝 index.html
```html
<!DOCTYPE html>
<html>
<head>
  <title>Simple TS Example</title>
</head>
<body>
  <h1>TypeScript Demo</h1>

  <button onclick="sayHello()">Click Me</button>
  <p id="output"></p>

  <!-- Load the compiled JS file -->
  <script src="hello.js"></script>
</body>
</html>
```

---

## 📝 hello.ts
```ts
function sayHello(): void {
  const message: string = "Hello Sathish, this is TypeScript!";
  const output = document.getElementById("output");
  if (output) {
    output.textContent = message;
  }
}
```

---

## ⚙️ Setup Instructions

### 1. Install TypeScript
Open your terminal in VS Code and run:
```bash
npm install -g typescript
```

### 2. Compile TypeScript
Run the TypeScript compiler on your file:
```bash
tsc hello.ts
```
This generates `hello.js` in the same folder.

### 3. Run in Browser
Open `index.html` in your browser.  
Click the button → you’ll see:
```
Hello Sathish, this is TypeScript!
```

---
