Reading & Updating Content:
textContent
innerHTML

### textContent
👉 What it does
Reads or sets only text
Treats everything as plain text
Ignores HTML tags

### innerHTML
👉 What it does
Reads or sets HTML inside an element
Parses strings as real HTML
Can create elements

